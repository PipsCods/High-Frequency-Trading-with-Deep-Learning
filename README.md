# High-Frequency-Trading-with-Deep-Learning

Acerbi pallone d'oro.


1. create this folder data/Targets/high_10m
2. Put the daily .csv.gz files inside the folder
3. Run the notebook data_loading.ipynb to load the data and save them as .parquet file


IMPORTANT: there no more a column called "RETURN" now there two column one for the overnight return and one without the overnight return, Re-Run the data_loading.ipynb to get the new data 